export interface navbarmodal {
  modal: boolean;
  settingModal: () => void;
}
export interface dragdropArea {
  file: any;
  dropFile: (file: any) => void;
}

export interface notasInfo {
  file: any;
}
